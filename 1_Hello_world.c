// As tradition of coding, your first program should be printing "Hello world"
// So I want you to compile and run this file so see what it does



// this is the standard library, this has tools to code in C
#include <stdio.h>

// this is the main function which is run when the program is ran
int main() {

    // this is print format (stronger print, don't worry about it for now) and prints to the terminal
    printf("hello world"); //make sure to add semi-colon, it tells the compiler "this line is done"

    // something for older computers but is not necessary
    return 0;
}